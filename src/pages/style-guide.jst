<!doctype html>
<html class="no-js" lang="">
    {%page include "../parts/head.html" %}
    <body>
        <div class="body">
            {%page include "../parts/header.html" %}
            <div class="content">
                <h1>{{@page name }}</h1>
                {%page autoescape false %}{{@page content|safe }}{%page endautoescape %}
                <h2>Menu</h2>
                {%page include "../parts/menu.html" %}
            </div>
        </div>
        {%page include "../parts/footer-scripts.html" %}
    </body>
</html>